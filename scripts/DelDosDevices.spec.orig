# -*- mode: python -*-

block_cipher = None


a = Analysis(['./scripts\\.\\..\\src\\main.pyw'],
             pathex=['./scripts\\.'],
             hiddenimports=[],
             hookspath=['./scripts\\.\\hooks'],
             runtime_hooks=None,
             cipher=block_cipher)


trees = list()
#trees.extend(Tree('src/uimods/views', prefix='uimods/views/'))
#trees.extend(Tree('src/uimods/controllers', prefix='uimods/controllers/'))
# tree = Tree('src/uimods/controllers', prefix='uimods/controllers/')
# tree2 = Tree('src/uimods/views', prefix='uimods/views/')

pyz = PYZ(a.pure,
             cipher=block_cipher)

exe = EXE(pyz,
          a.scripts + [('O','','OPTION')],
          trees,
          a.binaries,
          a.zipfiles,
          a.datas,
          name='DelDosDevices.exe',
          debug=False,
          strip=None,
          upx=False,
          console=False )
